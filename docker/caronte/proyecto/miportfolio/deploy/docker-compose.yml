services:
  web:
    build:
      context: ${CONTEXTO}
      dockerfile: ./dockerfiles/miportfolio/miporfolio
      args:
        - PROYECTO=${PROYECTO}
    container_name: ctportfolio
    ports:
      - "${PORT_WEB}:80" 
    env_file:
      - ./.env
