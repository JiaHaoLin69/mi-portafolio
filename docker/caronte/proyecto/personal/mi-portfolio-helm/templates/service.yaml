apiVersion: v1
kind: Service
metadata:
  name: {{ include "mi-portfolio-helm.fullname" . }}
  labels:
    {{- include "mi-portfolio-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mi-portfolio-helm.selectorLabels" . | nindent 4 }}
